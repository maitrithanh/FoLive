<Window x:Class="FoLive.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FoLive - Livestream 24/7 Manager" 
        Height="800" Width="1400"
        MinHeight="600" MinWidth="1200"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Title -->
        <Border Grid.Row="0" Background="#0078D4" Padding="15,10">
            <TextBlock Text="FoLive Stream Pro v1.0.0" 
                      FontSize="20" 
                      FontWeight="Bold" 
                      Foreground="White" 
                      VerticalAlignment="Center"/>
        </Border>

        <!-- Tab Navigation -->
        <TabControl Grid.Row="1" 
                   Background="#F5F5F5" 
                   BorderThickness="0"
                   SelectedIndex="0">
            <TabItem Header="Cài đặt chung" FontSize="12" Padding="15,8">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️" Margin="0,0,5,0"/>
                            <TextBlock Text="Cài đặt chung"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
            </TabItem>
            <TabItem Header="Cài đặt kết xuất" FontSize="12" Padding="15,8">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️" Margin="0,0,5,0"/>
                            <TextBlock Text="Cài đặt kết xuất"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
            </TabItem>
            <TabItem Header="Website" FontSize="12" Padding="15,8">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🌐" Margin="0,0,5,0"/>
                            <TextBlock Text="Website"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
            </TabItem>
            <TabItem Header="Liên hệ hỗ trợ" FontSize="12" Padding="15,8">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🎧" Margin="0,0,5,0"/>
                            <TextBlock Text="Liên hệ hỗ trợ"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
            </TabItem>
        </TabControl>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="0" 
                       Orientation="Horizontal" 
                       Margin="0,0,0,15"
                       HorizontalAlignment="Left">
                <Button Content="➕ Thêm Luồng" 
                       Click="AddStream_Click"
                       Padding="15,8"
                       FontSize="12"
                       Margin="0,0,10,0"
                       Background="#28A745"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"/>
                <Button Content="➕ Thêm Luồng quay màn hình" 
                       Click="AddScreenStream_Click"
                       Padding="15,8"
                       FontSize="12"
                       Margin="0,0,10,0"
                       Background="#17A2B8"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"/>
                <Button Content="▶️ Chạy toàn bộ" 
                       Click="StartAllStreams_Click"
                       Padding="15,8"
                       FontSize="12"
                       Margin="0,0,10,0"
                       Background="#007BFF"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"/>
                <Button Content="⏹️ Dừng toàn bộ" 
                       Click="StopAllStreams_Click"
                       Padding="15,8"
                       FontSize="12"
                       Background="#DC3545"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"/>
            </StackPanel>

            <!-- Streams DataGrid -->
            <DataGrid x:Name="StreamsDataGrid" 
                     Grid.Row="1"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     SelectionMode="Single"
                     HeadersVisibility="Column"
                     GridLinesVisibility="All"
                     AlternatingRowBackground="#F9F9F9"
                     RowBackground="White"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <!-- # Column -->
                    <DataGridTextColumn Header="#" 
                                       Binding="{Binding Index}" 
                                       Width="40"/>
                    
                    <!-- Run Column -->
                    <DataGridTemplateColumn Header="Run" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="▶" 
                                       Click="StartStream_Click" 
                                       Tag="{Binding StreamId}"
                                       Background="#28A745"
                                       Foreground="White"
                                       BorderThickness="0"
                                       Padding="8,4"
                                       FontSize="10"
                                       Cursor="Hand"
                                       ToolTip="Bắt đầu stream"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Stop Column -->
                    <DataGridTemplateColumn Header="Stop" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="⏹" 
                                       Click="StopStream_Click" 
                                       Tag="{Binding StreamId}"
                                       Background="#DC3545"
                                       Foreground="White"
                                       BorderThickness="0"
                                       Padding="8,4"
                                       FontSize="10"
                                       Cursor="Hand"
                                       ToolTip="Dừng stream"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Name Column -->
                    <DataGridTextColumn Header="Name" 
                                       Binding="{Binding StreamId}" 
                                       Width="120"/>
                    
                    <!-- Input Column -->
                    <DataGridTextColumn Header="Input" 
                                       Binding="{Binding SourceDisplay}" 
                                       Width="150"/>
                    
                    <!-- Output Column -->
                    <DataGridTextColumn Header="Output" 
                                       Binding="{Binding OutputDisplay}" 
                                       Width="120"/>
                    
                    <!-- Time Column -->
                    <DataGridTextColumn Header="Time" 
                                       Binding="{Binding TimeDisplay}" 
                                       Width="100"/>
                    
                    <!-- Bitrate Column -->
                    <DataGridTextColumn Header="Bitrate" 
                                       Binding="{Binding BitrateDisplay}" 
                                       Width="120"/>
                    
                    <!-- FPS Column -->
                    <DataGridTextColumn Header="FPS" 
                                       Binding="{Binding FPSDisplay}" 
                                       Width="60"/>
                    
                    <!-- Speed Column -->
                    <DataGridTextColumn Header="Speed" 
                                       Binding="{Binding SpeedDisplay}" 
                                       Width="70"/>
                    
                    <!-- Status Column -->
                    <DataGridTextColumn Header="Status" 
                                       Binding="{Binding StatusDisplay}" 
                                       Width="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{Binding StatusColor}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Log Column -->
                    <DataGridTemplateColumn Header="Log" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="📁" 
                                       Click="ViewLog_Click" 
                                       Tag="{Binding StreamId}"
                                       Background="Transparent"
                                       BorderThickness="0"
                                       Padding="8,4"
                                       FontSize="14"
                                       Cursor="Hand"
                                       ToolTip="Xem log"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Edit Column -->
                    <DataGridTemplateColumn Header="Edit" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="✏️" 
                                       Click="EditStream_Click" 
                                       Tag="{Binding StreamId}"
                                       Background="Transparent"
                                       BorderThickness="0"
                                       Padding="8,4"
                                       FontSize="14"
                                       Cursor="Hand"
                                       ToolTip="Chỉnh sửa"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Delete Column -->
                    <DataGridTemplateColumn Header="Delete" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="❌" 
                                       Click="DeleteStream_Click" 
                                       Tag="{Binding StreamId}"
                                       Background="Transparent"
                                       Foreground="#DC3545"
                                       BorderThickness="0"
                                       Padding="8,4"
                                       FontSize="14"
                                       Cursor="Hand"
                                       ToolTip="Xóa"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Footer Status Bar -->
        <Border Grid.Row="3" 
               Background="#E0E0E0" 
               BorderBrush="#CCCCCC" 
               BorderThickness="0,1,0,0"
               Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- User Info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="👤" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="User - Vĩnh viễn" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Change Password -->
                <Button Grid.Column="1" 
                       Content="🔑 Đổi mật khẩu" 
                       Click="ChangePassword_Click"
                       Background="Transparent"
                       BorderThickness="0"
                       Padding="10,5"
                       FontSize="12"
                       Margin="0,0,15,0"
                       Cursor="Hand"/>
                
                <!-- Renew -->
                <Button Grid.Column="2" 
                       Content="🛒 Gia hạn ngay" 
                       Click="Renew_Click"
                       Background="Transparent"
                       BorderThickness="0"
                       Padding="10,5"
                       FontSize="12"
                       Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>
