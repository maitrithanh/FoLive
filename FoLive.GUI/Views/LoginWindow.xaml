<Window x:Class="FoLive.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" 
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Login" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Username:" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameTextBox"/>
            </StackPanel>

            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Password:" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Login" Click="Login_Click" Padding="20,5" IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Padding="20,5"/>
        </StackPanel>
    </Grid>
</Window>
