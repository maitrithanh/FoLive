<Window x:Class="FoLive.Views.PaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Payment" 
        Height="500" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Payment" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Payment Method:" Margin="0,0,0,5"/>
                <ComboBox x:Name="PaymentMethodComboBox" SelectedIndex="0">
                    <ComboBoxItem Content="Credit Card"/>
                    <ComboBoxItem Content="PayPal"/>
                    <ComboBoxItem Content="Bank Transfer"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Amount:" Margin="0,0,0,5"/>
                <TextBox x:Name="AmountTextBox"/>
            </StackPanel>

            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Description:" Margin="0,0,0,5"/>
                <TextBox x:Name="DescriptionTextBox" TextWrapping="Wrap" AcceptsReturn="True" Height="100"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Padding="20,5" Margin="0,0,10,0"/>
            <Button Content="Pay" Click="Pay_Click" Padding="20,5" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
